let RTMClient,SLACK_OAUTH_TOKEN;_0e1‍.w("@slack/rtm-api",[["RTMClient",["RTMClient"],function(v){RTMClient=v}]]);_0e1‍.w("./constant",[["SLACK_OAUTH_TOKEN",["SLACK_OAUTH_TOKEN"],function(v){SLACK_OAUTH_TOKEN=v}]]);


const rtm  = new RTMClient(SLACK_OAUTH_TOKEN);


// we actually start the client here then we will add a chache
// to log out there if we get an error
rtm.start()
    .catch(_0e1‍.g.console.error);

rtm.on("ready",async () => {
    console.log("bot started")
});